# Определяем массив из 8 элементов
array = [5, 12, 15, 20, 3, 18, 10, 25]

# Заменяем элементы меньше 15 их удвоенными значениями
for i in range(len(array)):
    if array[i] < 15:
        array[i] *= 2

# Выводим преобразованный массив
print("Преобразованный массив:", array)
